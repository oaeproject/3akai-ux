/*!
 * Copyright 2012 Sakai Foundation (SF) Licensed under the
 * Educational Community License, Version 2.0 (the "License"); you may
 * not use this file except in compliance with the License. You may
 * obtain a copy of the License at
 *
 *     http://www.osedu.org/licenses/ECL-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an "AS IS"
 * BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express
 * or implied. See the License for the specific language governing
 * permissions and limitations under the License.
 */

define(['jquery', 'oae.core'], function($, oae) {

    return function(uid, showSettings, widgetData) {

        var $rootel = $('#' + uid);

        var infinityScroll = false;

        /**
         * Render the default template when no results are found. This function will
         * be called by the infinite scroll plugin
         */
        var handleEmptyResultList = function() {
            $('#library_create', $rootel).hide();
            oae.api.util.renderTemplate($('#library_noresults_template'), null, $('.oae-list', $rootel));
        };

        var getLibrary = function() {
            // Disable the previous infinite scroll
            if (infinityScroll) {
                infinityScroll.kill();
            }

            var query = $.bbq.getState('q');
            $('#search_query', $rootel).val(query);

            var url = '/api/content/library/' + widgetData.principalId;
            if (query) {
                url = '/api/search/library/' + widgetData.principalId;
            }

            // Set up the infinite scroll for the list of search results
            infinityScroll = $('.oae-list', $rootel).infiniteScroll(url, {
                'limit': 12,
                'q': query
            }, '#library_template', {
                'postProcessor': function(data) {
                    if (widgetData.canManage) {
                        $('#library_create', $rootel).show();
                    }
                    return data;
                },
                'emptyListProcessor': handleEmptyResultList
            });
        };

        var addBinding = function() {
            $(window).on('done.addcontent.oae', getLibrary);

            $rootel.on('submit', '#library_search_form', function() {
                $.bbq.pushState({'q': $('#search_query', $(this)).val()});
                getLibrary();
                return false;
            });
        };

        var doInit = function() {
            addBinding();
            getLibrary();
        };

        doInit();
    };
});
